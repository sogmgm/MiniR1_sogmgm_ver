# Mini-R1: DeepSeek R1 "Aha Moment" Reproduction

> **단일 GPU** RunPod 환경에서 경량화된 GRPO 학습으로 DeepSeek R1의 추론 능력 재현

## 🎯 프로젝트 목표

Countdown Game을 이용해 강화학습(GRPO)으로 모델이 스스로:
- 추론 과정(`<think>`)을 학습
- 자가 검증 능력 습득
- 수학적 문제 해결 패턴 발견

### 📊 학습 과정 도식화

```
┌─────────────────────────────────────────────────────────────────┐
│  1단계: 데이터 준비 (Dataset Preparation)                        │
└─────────────────────────────────────────────────────────────────┘
  │
  │  python scripts/dataset_prep.py --num_samples 5000
  │
  ▼
원본: {nums: [19,36,55,7], target: 65}
  │
  │  R1 프롬프트 변환
  │
  ▼
변환: "Using numbers [19,36,55,7], create equation = 65. <think>"

┌─────────────────────────────────────────────────────────────────┐
│  2단계: GRPO 학습 (Training Loop)                               │
└─────────────────────────────────────────────────────────────────┘
  │
  ▼
[모델 생성] → "추론...    </think>\n<answer>55+36-7-19</answer>"
  │
  ▼
[보상 계산]
  ├─ Format Reward:   1.0 (형식 올바름)
  └─ Equation Reward: 1.0 (계산 정답)
  │
  ▼
[총 보상: 2.0] → 모델 업데이트 (높은 보상 방향으로)
  │
  │  (200 steps 반복)
  │
  ▼

┌─────────────────────────────────────────────────────────────────┐
│  3단계: 학습 진행 단계별 변화                                     │
└─────────────────────────────────────────────────────────────────┘

Step 0-50:    포맷 학습 (15-20% 정확도)
              ✓ <think></think> 형식 습득
              
Step 50-100:  초기 추론 (20-30% 정확도)
              ✓ 간단한 수식 생성 시작
              
Step 100-150: 패턴 인식 (30-40% 정확도)
              ✓ 숫자 조합 패턴 학습
              
Step 150-200: 수렴 (40-50% 정확도)
              ✓ 안정적인 추론 능력

┌─────────────────────────────────────────────────────────────────┐
│  4단계: 평가 (Evaluation)                                        │
└─────────────────────────────────────────────────────────────────┘
  │
  │  python scripts/evaluate.py --checkpoint checkpoints/...
  │
  ▼
테스트 데이터로 최종 성능 평가
  ├─ Format 정확도: ~90%
  ├─ Equation 정확도: ~40-50%
  └─ 성공 샘플 분석
```

## 🔧 기술 스택 및 시스템 사양

### 핵심 기술
- **패키지 관리**: UV (빠른 Python 패키지 관리자)
- **모델**: Qwen2.5-1.5B/3B-Instruct (메모리 효율화)
- **학습 방법**: GRPO (Group Relative Policy Optimization)
- **최적화**: QLoRA (4-bit), Flash Attention 2, Gradient Checkpointing
- **환경**: **단일 GPU** (Multi-GPU 지원 없음)

### 💻 GPU 요구사항 (단일 GPU 전용)

| GPU 모델 | VRAM | Qwen 1.5B | Qwen 3B | 권장도 |
|----------|------|-----------|---------|--------|
| RTX 4090 | 24GB | ✅ 최적 | ✅ 최적 | ⭐⭐⭐⭐⭐ |
| RTX A5000 | 24GB | ✅ 최적 | ✅ 최적 | ⭐⭐⭐⭐⭐ |
| RTX 3090 | 24GB | ✅ 좋음 | ✅ 좋음 | ⭐⭐⭐⭐ |
| L4 | 24GB | ✅ 가능 | ✅ 가능 | ⭐⭐⭐ |
| RTX 4080 | 16GB | ✅ 가능 | ⚠️ 주의 | ⭐⭐⭐ |

**⚠️ 중요**: 이 프로젝트는 **단일 GPU 환경**만 지원합니다. Multi-GPU / Distributed Training은 구현되지 않았습니다.

### 📦 필수 소프트웨어
- Python 3.10+
- CUDA 12.1+
- UV 패키지 관리자
- 50GB 이상 디스크 공간

## 📁 프로젝트 구조

```
MiniR1/
├── README.md                    # 📖 프로젝트 전체 가이드 (이 문서)
├── RUNPOD_SETUP.md              # 🚀 RunPod 상세 설정 가이드
├── TENSORBOARD_GUIDE.md         # 📊 TensorBoard 사용법
├── PROGRESS.md                  # 📝 개발 진행 상황 기록
├── pyproject.toml               # 📦 UV 패키지 설정 및 의존성
│
├── configs/                     # ⚙️ 설정 파일
│   └── training_config.yaml     #    학습 하이퍼파라미터 (모델, 배치, GRPO 등)
│
├── scripts/                     # 🛠️ 실행 스크립트
│   ├── dataset_prep.py          #    데이터셋 준비 (HF → R1 형식 변환)
│   ├── rewards.py               #    보상 함수 (format + equation)
│   ├── train_grpo.py            #    GRPO 학습 메인 스크립트
│   ├── evaluate.py              #    모델 평가 및 테스트
│   └── check_environment.py     #    환경 설정 확인
│
├── notebooks/                   # 📓 Jupyter 노트북
│   └── data_exploration.ipynb   #    데이터 탐색 및 실험
│
├── .cache/                      # 💾 캐시 디렉토리 (생성됨)
│   └── datasets/                #    준비된 데이터셋 저장
│       ├── train_countdown_r1.json      # 학습 데이터 (4,500 샘플)
│       ├── test_countdown_r1.json       # 테스트 데이터 (500 샘플)
│       └── dataset_metadata.json        # 데이터셋 메타정보
│
├── logs/                        # 📊 학습 로그 (생성됨)
│   ├── tensorboard/             #    TensorBoard 로그 파일
│   └── training_metrics.json    #    JSON 형식 메트릭
│
├── checkpoints/                 # 💾 모델 체크포인트 (생성됨)
│   └── qwen-r1-countdown/       #    학습된 모델 저장
│       ├── checkpoint-50/       #    50 step 체크포인트
│       ├── checkpoint-100/      #    100 step 체크포인트
│       ├── checkpoint-150/      #    150 step 체크포인트
│       └── checkpoint-200/      #    최종 체크포인트 (~3GB)
│
└── completion_samples/          # 📝 생성 샘플 (생성됨)
    ├── step_0025_all.txt        #    전체 샘플 (성공+실패)
    ├── step_0025_success.txt    #    성공 샘플만
    ├── step_0050_all.txt
    ├── step_0050_success.txt
    └── ...                      #    25 step마다 자동 저장
```

### 주요 파일 설명

**설정 파일**:
- `training_config.yaml`: 모든 학습 파라미터 (모델, 배치, GRPO, 보상 등)

**핵심 스크립트**:
- `dataset_prep.py`: Countdown 데이터를 R1 프롬프트 형식으로 변환
- `rewards.py`: Format과 Equation 보상 함수 구현
- `train_grpo.py`: GRPO 학습 루프 및 체크포인트 관리
- `evaluate.py`: 테스트 데이터로 모델 성능 평가

**생성되는 디렉토리**:
- `.cache/datasets/`: 전처리된 데이터셋 (~50MB)
- `logs/tensorboard/`: TensorBoard 시각화 데이터
- `checkpoints/`: LoRA 어댑터 체크포인트 (각 ~3GB)
- `completion_samples/`: 학습 중 생성된 텍스트 샘플

## 🚀 빠른 시작 가이드

### 🎮 RunPod 구동 전체 과정

#### 1️⃣ Pod 생성 및 접속
```bash
# RunPod에서 RTX 4090/A5000 선택
# 템플릿: PyTorch 2.4 + CUDA 12.1
# 볼륨: 최소 50GB
# Web Terminal 또는 SSH로 접속
```

#### 2️⃣ 프로젝트 클론
```bash
cd /workspace
git clone https://github.com/YOUR_USERNAME/MiniR1.git
cd MiniR1
```

#### 3️⃣ 환경 설정 (5-10분 소요)
```bash
# UV 설치
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.cargo/env

# PyTorch + CUDA 설치 (uv add로 한번에 설치)
uv add torch torchvision torchaudio --index https://download.pytorch.org/whl/cu121

# 프로젝트 의존성 동기화
uv sync

# Flash Attention 설치 (선택, 20% 속도 향상)
uv add flash-attn --no-build-isolation
```

#### 4️⃣ Hugging Face 인증
```bash
# 방법 1: 대화형 로그인
uv run huggingface-cli login

# 방법 2: 토큰으로 로그인
export HF_TOKEN="your_hf_token_here"
```

#### 5️⃣ GPU 확인
```bash
# GPU 상태 확인
nvidia-smi

# PyTorch CUDA 확인
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPU: {torch.cuda.get_device_name(0)}')"
```

#### 6️⃣ 데이터셋 준비 (~2-3분)
```bash
uv run python scripts/dataset_prep.py --num_samples 5000
# 생성 파일:
# - .cache/datasets/train_countdown_r1.json (4,500 샘플)
# - .cache/datasets/test_countdown_r1.json (500 샘플)
```

#### 7️⃣ 학습 시작 (3-4시간 소요)
```bash
# 단일 GPU 학습
uv run python scripts/train_grpo.py --config configs/training_config.yaml

# 백그라운드 실행 (권장)
nohup uv run python scripts/train_grpo.py --config configs/training_config.yaml > training.log 2>&1 &

# 로그 실시간 확인
tail -f training.log
```

#### 8️⃣ TensorBoard 모니터링 (선택)
```bash
# 별도 터미널에서
tensorboard --logdir=logs/tensorboard --host=0.0.0.0 --port=6006

# RunPod 포트 포워딩 설정 후
# 브라우저에서: http://localhost:6006
```

#### 9️⃣ 학습 진행 상황 확인
```bash
# 실시간 로그 확인
tail -f training.log

# 체크포인트 확인
ls -lh checkpoints/qwen-r1-countdown/

# 생성 샘플 확인
cat completion_samples/step_0050_success.txt
cat completion_samples/step_0100_success.txt
```

#### 🔟 평가 실행
```bash
# 최종 체크포인트 평가
uv run python scripts/evaluate.py \
  --checkpoint checkpoints/qwen-r1-countdown/checkpoint-200 \
  --num_samples 100
```

### ⏱️ 전체 소요 시간 (RTX 4090 기준)
- 환경 설정: ~10분
- 데이터 준비: ~3분
- 학습 (200 steps): ~3-4시간
- 평가: ~5-10분
- **총: 약 4시간**

## 💾 GPU 메모리 최적화 전략

### 메모리 절감 기법

| 설정 | 기본값 → 최적화 | 메모리 절감 | 성능 영향 |
|------|----------------|------------|----------|
| **모델 크기** | 3B → 1.5B | ~50% | 약간 감소 |
| **양자화** | FP16 → 4-bit QLoRA | ~75% | 거의 없음 |
| **Gradient Checkpointing** | False → True | ~30% | 10-15% 느림 |
| **Batch Size** | 4 → 1 | ~75% | Grad Accum으로 보완 |
| **Max Completion** | 1024 → 512 | ~30% | 충분함 |
| **Flash Attention 2** | False → True | ~20% | 20% 빠름 |
| **Num Generations** | 4 → 2 | ~50% | 약간 감소 |

### 실제 VRAM 사용량 (Qwen 1.5B 기준)

```
┌─────────────────────────────────────────┐
│  학습 단계별 VRAM 사용량                 │
└─────────────────────────────────────────┘

모델 로딩:      ~4GB   ████░░░░░░░░░░░░░░░░
Forward Pass:   ~8GB   ████████░░░░░░░░░░░░
Generation:     ~12GB  ████████████░░░░░░░░
Backward Pass:  ~14GB  ██████████████░░░░░░
Peak:           ~16GB  ████████████████░░░░

총 필요 VRAM: 14-16GB (여유 포함 18GB 권장)
```

### Qwen 3B 사용 시
- 필요 VRAM: ~18-20GB
- 권장 GPU: RTX 4090, A5000 (24GB)
- 추가 메모리 절약: `gradient_accumulation_steps: 16`

## 📊 학습 모니터링 및 로깅

### 1️⃣ 실시간 진행 상황 (터미널)
```bash
tail -f training.log

# 출력 예시:
# Step 25/200 | Loss: 1.234 | Format: 0.45 | Equation: 0.12 | Combined: 0.57
# Step 50/200 | Loss: 0.987 | Format: 0.82 | Equation: 0.23 | Combined: 1.05
# Step 100/200 | Loss: 0.654 | Format: 0.95 | Equation: 0.38 | Combined: 1.33
```

### 2️⃣ TensorBoard (선택적, 현재 활성화됨)

#### TensorBoard 실행
```bash
# 별도 터미널에서
tensorboard --logdir=logs/tensorboard --host=0.0.0.0 --port=6006

# RunPod: 포트 포워딩 설정
# 로컬: http://localhost:6006
```

#### 주요 메트릭
```
📈 train/loss              - 학습 손실 (감소 추세 확인)
📈 train/rewards/format    - 포맷 보상 (~50 steps에 0.9+)
📈 train/rewards/equation  - 수식 보상 (~100 steps부터 증가)
📈 train/rewards/combined  - 총 보상 (목표: 1.5-1.8)
📈 train/learning_rate     - 학습률 (Cosine 감소)
```

#### TensorBoard 비활성화 (메모리 절약)
```yaml
# configs/training_config.yaml
training:
  report_to: []  # ["tensorboard"] → []
```

### 3️⃣ 생성 샘플 확인
```bash
# 25 step마다 자동 저장
ls -lh completion_samples/

# 성공 샘플 확인
cat completion_samples/step_0050_success.txt
cat completion_samples/step_0100_success.txt
cat completion_samples/step_0150_success.txt
cat completion_samples/step_0200_success.txt

# 전체 샘플 확인 (성공+실패)
cat completion_samples/step_0200_all.txt
```

### 4️⃣ 체크포인트 관리
```bash
# 자동 저장 (50 step마다)
checkpoints/qwen-r1-countdown/
├── checkpoint-50/
├── checkpoint-100/
├── checkpoint-150/
└── checkpoint-200/

# 최대 4개 유지 (save_total_limit: 4)
# 오래된 체크포인트 자동 삭제됨
```

### 5️⃣ 학습 중단 및 재개
```bash
# 중단된 학습 재개 (자동으로 마지막 체크포인트부터)
uv run python scripts/train_grpo.py \
  --config configs/training_config.yaml \
  --resume_from_checkpoint checkpoints/qwen-r1-countdown/checkpoint-100
```

### 6️⃣ PROGRESS.md 업데이트
```bash
# 학습 진행사항 수동 기록 (선택적)
# - 각 체크포인트별 성공률
# - 생성 샘플 예시
# - GPU 메모리 사용량
# - 특이사항 메모
```

## 📝 주요 특징 및 제한사항

### ✅ 경량화 최적화
- **작은 데이터셋**: 5k 샘플 (원본 50k 대비 90% 감소)
- **짧은 학습**: 200 steps (원본 450 steps 대비 55% 감소)
- **작은 모델**: 1.5B/3B (메모리 효율)
- **메모리 최적화**: 4-bit QLoRA + Flash Attention
- **배치 최적화**: Batch size 1 + Gradient Accumulation

### ✅ RunPod 친화적
- **UV 패키지 관리**: 빠른 의존성 설치
- **단일 GPU 최적화**: 12-16GB VRAM에서 실행 가능
- **자동 체크포인트**: 50 step마다 자동 저장
- **중단/재개 지원**: 언제든지 중단 후 이어서 학습 가능
- **샘플 자동 저장**: 25 step마다 생성 샘플 저장

### ⚠️ 제한사항
- **단일 GPU 전용**: Multi-GPU / Distributed Training 미지원
- **메모리 한계**: 16GB VRAM 미만 GPU는 Qwen 1.5B 권장
- **학습 시간**: RTX 4090 기준 3-4시간 소요
- **성능 상한**: Countdown Game에 특화 (범용성 제한)
- **TensorBoard**: 활성화 시 약간의 메모리 오버헤드 (~500MB)

### 🔄 확장 가능성
- [ ] Multi-GPU 지원 (DDP/FSDP)
- [ ] 다른 수학 문제 데이터셋 (GSM8K, MATH 등)
- [ ] 더 큰 모델 (Qwen 7B, 14B)
- [ ] 프롬프트 최적화 실험
- [ ] 보상 함수 개선

## 🎓 학습 목표 및 기대 결과

### 단계별 학습 진행

```
┌──────────────────────────────────────────────────────────────────┐
│  학습 단계별 모델 행동 변화                                        │
└──────────────────────────────────────────────────────────────────┘

Step 0-50: 형식 학습 단계
───────────────────────────────────────
보상: Format 0.0 → 0.8-0.9
      Equation 0.0 → 0.05-0.10

모델 행동:
  ❌ "The answer is 55 + 36"
  ❌ "<think> Let me <think> ..."  (중첩 태그)
  ✅ "... </think>\n<answer> 55 + 36 </answer>"

핵심: <think></think><answer></answer> 구조 학습


Step 50-100: 초기 추론 단계
───────────────────────────────────────
보상: Format 0.9+
      Equation 0.10 → 0.20

모델 행동:
  ✅ 형식은 거의 완벽
  ⚠️ 수식은 종종 틀림
  예: "55 + 36 + 7 + 19" (= 117, 목표 65)
  예: "55 - 36" (일부 숫자만 사용)

핵심: 간단한 수식 생성 시작


Step 100-150: 패턴 인식 단계
───────────────────────────────────────
보상: Format 0.95+
      Equation 0.20 → 0.35

모델 행동:
  ✅ 모든 숫자 사용하기 시작
  ✅ 목표값에 가까운 수식 생성
  ⚠️ 아직 완벽하지 않음
  예: "55 + 36 - 7 - 20" (근사값)

핵심: 숫자 조합 패턴 학습


Step 150-200: 수렴 단계
───────────────────────────────────────
보상: Format 0.95+
      Equation 0.35 → 0.45

모델 행동:
  ✅ 안정적인 형식
  ✅ 40-50% 정확도
  ✅ 추론 과정 개선
  예: "<think> 
       First, 55 + 36 = 91
       Then, 91 - 7 = 84
       Finally, 84 - 19 = 65
       </think>
       <answer> 55 + 36 - 7 - 19 </answer>"

핵심: 안정적 추론 능력 확보
```

### 최종 성능 예상

| 메트릭 | 목표 | 설명 |
|--------|------|------|
| **Format 정확도** | 90-95% | 형식을 올바르게 생성 |
| **Equation 정확도** | 40-50% | 수학적으로 정답 |
| **Combined Score** | 1.5-1.8 | 총 보상 (최대 2.0) |
| **학습 시간** | 3-4시간 | RTX 4090 기준 |
| **체크포인트 크기** | ~3GB | LoRA 어댑터 포함 |

### 성능 비교

```
┌────────────────────────────────────────┐
│  원본 vs 경량화 버전                    │
└────────────────────────────────────────┘

                   원본      경량화
────────────────────────────────────────
데이터셋         50,000     5,000 샘플
학습 스텝           450       200 steps
모델 크기            3B      1.5B/3B
학습 시간         12-16h      3-4h
VRAM 사용        20-24GB    14-16GB
정확도           50-60%     40-50%
────────────────────────────────────────
```

### 개선 방법

1. **데이터 증가**: 5k → 10k-20k 샘플
2. **학습 연장**: 200 → 400-600 steps
3. **모델 업그레이드**: 1.5B → 3B → 7B
4. **하이퍼파라미터 튜닝**: Learning rate, Beta 조정
5. **보상 함수 개선**: 부분 점수 부여, Curriculum Learning

## � 문제 해결 (Troubleshooting)

### 1. CUDA Out of Memory (OOM)
```bash
# 증상: RuntimeError: CUDA out of memory

# 해결 방법 1: 모델 크기 축소
# configs/training_config.yaml
model:
  name: "Qwen/Qwen2.5-1.5B-Instruct"  # 3B → 1.5B

# 해결 방법 2: Gradient Accumulation 증가
training:
  gradient_accumulation_steps: 16  # 8 → 16

# 해결 방법 3: Completion 길이 축소
grpo:
  max_completion_length: 256  # 512 → 256

# 해결 방법 4: Generation 수 감소
grpo:
  num_generations: 1  # 2 → 1
```

### 2. TensorBoard 메모리 오버헤드
```bash
# TensorBoard 비활성화
# configs/training_config.yaml
training:
  report_to: []  # ["tensorboard"] → []
  logging_steps: 50  # 25 → 50 (덜 자주 로깅)
```

### 3. 학습이 너무 느림
```bash
# Flash Attention 설치 확인
python -c "import flash_attn; print('Flash Attention 설치됨')"

# 설치 안됐다면
uv add flash-attn --no-build-isolation

# 또는 설정에서 비활성화 (느려지지만 호환성 높음)
# configs/training_config.yaml
model:
  attn_implementation: "eager"  # flash_attention_2 → eager
```

### 4. UV 명령어가 안 될 때
```bash
# PATH 설정
source $HOME/.cargo/env

# 또는 직접 실행
$HOME/.cargo/bin/uv --version

# bashrc/zshrc에 추가 (영구 설정)
echo 'source $HOME/.cargo/env' >> ~/.bashrc
```

### 5. Hugging Face 다운로드 실패
```bash
# 토큰 확인
echo $HF_TOKEN

# 토큰 재설정
uv run huggingface-cli login

# 프록시 사용 시
export HF_ENDPOINT="https://hf-mirror.com"
```

### 6. 학습이 멈춘 것 같을 때
```bash
# GPU 사용 확인
nvidia-smi

# 프로세스 확인
ps aux | grep train_grpo

# 로그 확인
tail -f training.log

# 마지막 체크포인트 확인
ls -lt checkpoints/qwen-r1-countdown/
```

### 7. 체크포인트 로드 실패
```bash
# 체크포인트 경로 확인
ls -lh checkpoints/qwen-r1-countdown/checkpoint-100/

# 재시작 시 경로 지정
uv run python scripts/train_grpo.py \
  --config configs/training_config.yaml \
  --resume_from_checkpoint checkpoints/qwen-r1-countdown/checkpoint-100
```

### 8. 데이터셋 로드 에러
```bash
# 캐시 삭제 후 재생성
rm -rf .cache/datasets/
uv run python scripts/dataset_prep.py --num_samples 5000

# 또는 HuggingFace 캐시 삭제
rm -rf ~/.cache/huggingface/datasets/
```

## 💰 비용 예상 (RunPod 기준)

### GPU별 예상 비용

| GPU | 시간당 비용 | 학습 시간 | 총 비용 | 권장도 |
|-----|------------|----------|---------|--------|
| RTX 4090 | $0.69 | 3-4시간 | **$2.50** | ⭐⭐⭐⭐⭐ |
| RTX A5000 | $0.50 | 4-6시간 | **$2.50** | ⭐⭐⭐⭐⭐ |
| RTX 3090 | $0.44 | 5-7시간 | **$2.60** | ⭐⭐⭐⭐ |
| L4 | $0.45 | 6-8시간 | **$3.00** | ⭐⭐⭐ |

### 비용 절감 팁
1. **Community Cloud 사용**: Secure Cloud보다 50% 저렴
2. **Spot Instance**: 추가 30-50% 할인 (불안정할 수 있음)
3. **학습 시간 단축**: 
   - Qwen 1.5B 사용 (3B 대비 30% 빠름)
   - Flash Attention 활성화 (20% 빠름)
   - 데이터셋 축소 (5k → 3k)
4. **불필요한 Pod 즉시 종료**

## 📚 참고 자료 및 문서

### 프로젝트 문서
- **[RUNPOD_SETUP.md](RUNPOD_SETUP.md)**: RunPod 상세 설정 가이드
- **[TENSORBOARD_GUIDE.md](TENSORBOARD_GUIDE.md)**: TensorBoard 사용법
- **[PROGRESS.md](PROGRESS.md)**: 개발 진행 상황 및 실험 결과

### 외부 참고자료
- **[원본 튜토리얼](https://www.philschmid.de/mini-deepseek-r1)**: Philipp Schmid의 Mini-DeepSeek R1 가이드
- **[DeepSeek R1 논문](https://arxiv.org/abs/2501.12948)**: 원본 연구 논문
- **[GRPO 논문](https://arxiv.org/abs/2402.03300)**: Group Relative Policy Optimization
- **[TRL Documentation](https://huggingface.co/docs/trl)**: Hugging Face TRL 라이브러리
- **[Qwen 2.5 모델](https://huggingface.co/Qwen)**: Qwen 모델 카드
- **[UV 패키지 관리자](https://github.com/astral-sh/uv)**: UV 공식 문서

### 관련 프로젝트
- **[DeepSeek-R1](https://github.com/deepseek-ai/DeepSeek-R1)**: 원본 DeepSeek R1
- **[OpenAI O1](https://openai.com/o1/)**: 추론 모델 비교
- **[Countdown Dataset](https://huggingface.co/datasets/Jiayi-Pan/Countdown-Tasks-3to4)**: 사용한 데이터셋

## 🤝 기여 및 라이선스

### 기여 방법
```bash
# 1. Fork 후 Clone
git clone https://github.com/YOUR_USERNAME/MiniR1.git

# 2. 브랜치 생성
git checkout -b feature/your-feature

# 3. 변경 사항 커밋
git commit -m "Add: your feature description"

# 4. Push 및 PR
git push origin feature/your-feature
```

### 개선 아이디어
- [ ] Multi-GPU 지원 추가
- [ ] 다른 수학 데이터셋 실험 (GSM8K, MATH)
- [ ] 보상 함수 개선 (부분 점수, Curriculum Learning)
- [ ] Wandb 통합
- [ ] Docker 컨테이너 제공
- [ ] 자동 하이퍼파라미터 튜닝

### 📄 라이선스
MIT License - 자유롭게 사용, 수정, 배포 가능

### 📧 연락처
- GitHub Issues: 버그 리포트 및 기능 요청
- Discussions: 질문 및 토론

---

## 🎉 시작하기

```bash
# 1. RunPod Pod 생성 (RTX 4090 권장)
# 2. 터미널 접속
cd /workspace

# 3. 빠른 시작
git clone https://github.com/YOUR_USERNAME/MiniR1.git
cd MiniR1
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.cargo/env
uv add torch torchvision torchaudio --index https://download.pytorch.org/whl/cu121
uv sync
uv run python scripts/dataset_prep.py --num_samples 5000
uv run python scripts/train_grpo.py --config configs/training_config.yaml

# 4. 학습 모니터링
tail -f training.log

# 5. 완료! 🎊
```

**예상 소요 시간**: 4시간 | **예상 비용**: $2.50

좋은 실험 되세요! 🚀


